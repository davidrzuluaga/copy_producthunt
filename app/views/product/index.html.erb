<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h1>Productos</h1>
        <div class="text-right">
        <%= link_to  "Pubicar Producto", new_product_path, class:"btn btn-primary" %>
        </div>
      <table class="table">
        <tr>
          <th>Id</th>
          <th>Nombre</th>
          <th>URL</th>
          <th>Descripción</th>
        </tr>
        <% @products.each do |product| %>
          <tr>
            <td><%= product.id %></td>
            <td><%= product.name %></td>
            <td><%= product.url %></td>
            <td><%= product.description %></td>
            <td><%= link_to  "Editar", edit_product_path(product) %> </td>
            <td><%= link_to  "Eliminar", product, method: :delete, data: {confirm: "seguro?"} %> </td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>